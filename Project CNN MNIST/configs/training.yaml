training:
epochs: 20

optimizer:
name: adam
lr: 0.001
weight_decay: 0.0001

scheduler:
name: reduce_on_plateau
factor: 0.5
patience: 2

loss:
name: cross_entropy

early_stopping:
patience: 5
min_delta: 0.001
